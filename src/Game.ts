/// <reference path="../node_modules/@types/p5/global.d.ts" />

export function setup() {
    createCanvas(windowWidth, windowHeight);
}

export function draw() {
    background(51);
}

export function keyPressed(code: number) {
}